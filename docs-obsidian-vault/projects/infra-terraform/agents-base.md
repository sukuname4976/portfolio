# Terraform

インフラストラクチャ定義。

## ルール

- `terraform apply` と `terraform destroy` は実行しない。適用は人間が行う
  - 実環境を書き換える操作で、取り消しが利かないものを含むため
  - エージェントが実行してよいのは `plan` までとする
- 変更は必ずコミットしてから適用する。未コミットのまま適用すると、
  環境にだけ存在してコードに残らない変更が生まれ、IaC が成立しなくなる
- 詳細な運用ルールは `infras/terraform/README.md` を参照
