{
  "name": "Swift/iOS Development Environment",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "sswg.swift-lang",
        "vadimcn.vscode-lldb"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "[swift]": {
          "editor.defaultFormatter": "sswg.swift-lang"
        }
      }
    }
  },

  "postCreateCommand": "swift --version || echo 'Note: Swift requires macOS. This container provides basic Swift support.'",
  
  "forwardPorts": [],
  
  "remoteEnv": {},
  
  "comment": "Note: Full iOS development requires macOS. This DevContainer provides basic Swift support but Xcode and iOS Simulator are not available in Linux containers."
}

