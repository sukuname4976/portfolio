# Kotlin/Android Development Environment Dockerfile
FROM mcr.microsoft.com/devcontainers/android:latest

# 追加のAndroidツールやKotlinツールをインストール（必要に応じて）
# RUN apt-get update && apt-get install -y \
#     additional-tools \
#     && rm -rf /var/lib/apt/lists/*

