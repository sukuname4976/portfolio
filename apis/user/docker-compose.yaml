services:
  user:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: user:latest
    ports:
      - "8000:8000"
    environment:
      - RUN_ENV=production
      - PORT=8000
    restart: unless-stopped
