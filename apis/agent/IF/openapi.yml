openapi: 3.0.0
info:
  title: Agent Service API
  version: 1.0.0
  description: Agent service API specification
servers:
  - url: http://localhost:8000
    description: Local development server
paths:
  /:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is running
          content:
            text/plain:
              schema:
                type: string

