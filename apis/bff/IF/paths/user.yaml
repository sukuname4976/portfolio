get:
  summary: Get user by ID (proxy to user service)
  operationId: getUser
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
      description: User ID
  responses:
    "200":
      description: Successful response
      content:
        application/json:
          schema:
            $ref: "../components/schemas/user.yaml#/UserResponse"
    "404":
      description: User not found
      content:
        application/json:
          schema:
            $ref: "../components/schemas/error.yaml#/ErrorResponse"
    "502":
      description: User service unavailable
      content:
        application/json:
          schema:
            $ref: "../components/schemas/error.yaml#/ErrorResponse"
